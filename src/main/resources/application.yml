# ==============================================
# CONFIGURATION DE LA BASE DE DONNÉES SQLite
# ==============================================
spring:
  datasource:
    url: jdbc:sqlite:./association.db
    driver-class-name: org.sqlite.JDBC
    username:
    password:
  
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        globally_quoted_identifiers: false
        id:
          new_generator_mappings: false
  
  # Configuration Flyway
  flyway:
    baseline-on-migrate: true
    enabled: true
    mixed: true
  
  # CONFIGURATION POUR AUTORISER L'OVERRIDE DES BEANS
  main:
    allow-bean-definition-overriding: true  # ← BON EMPLACEMENT !

  h2:
    console:
      enabled: false

  # Configuration du fuseau horaire
  jackson:
    time-zone: Africa/Lubumbashi

  # ==============================================
  # CONFIGURATION DES EMAILS (reste inchangé)
  # ==============================================
  mail:
    host: smtp.gmail.com
    port: 587
    username: vayanzaelishama.com
    password: El16.12.2003
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

  security:
    filter:
      order: 10

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# ==============================================
# CONFIGURATION DU STOCKAGE DE FICHIERS
# ==============================================
file:
  upload-dir: uploads/
  storage:
    type: local

cloud:
  aws:
    s3:
      bucket-name: votre-bucket-s3
      region: us-east-1

# ==============================================
# CONFIGURATION AWS
# ==============================================
aws:
  accessKeyId: votre-access-key
  secretKey: votre-secret-key
  region: us-east-1

# ==============================================
# CONFIGURATION TWILIO
# ==============================================
twilio:
  account:
    sid: vayanzaelishama@gmail.com
  auth:
    token: El16.12.2004
  phone:
    number: "+243991471988"

# ==============================================
# CONFIGURATION FIREBASE
# ==============================================
firebase:
  service:
    account:
      path: firebase-service-account.json

# ==============================================
# CONFIGURATION APNs
# ==============================================
apns:
  auth:
    key:
      path: apns-auth-key.p8
  team:
    id: votre-team-id
  key:
    id: votre-key-id
  topic: votre-app-bundle-id

# ==============================================
# CONFIGURATIONS DIVERSES
# ==============================================
server:
  port: 8089

# ====================================================
# CONFIGURATION SPRINGDOC OPENAPI (SWAGGER)
# ====================================================
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
    tagsSorter: alpha
    disable-swagger-default-url: true
  api-docs:
    path: /v3/api-docs
  show-actuator: true
  paths-to-match:
    - /api/**

# ====================================================
# CONFIGURATION LOGGING
# ====================================================
logging:
  level:
    com.org.Association-soft: DEBUG
    org.springframework: INFO
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE